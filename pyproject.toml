[project]
name = "change-ip"
version = "0.1.0"
description = "macOS app to show and change public IP"
requires-python = ">=3.12,<3.14"
dependencies = [
    "kivy>=2.3.0",
    "httpx>=0.27.0",
]

[project.scripts]
change-ip = "app:main"
